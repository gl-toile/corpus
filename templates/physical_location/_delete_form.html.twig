<form method="post" action="{{ path('app_physical_location_delete', {'id': physical_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ physical_location.id) }}">
    <button class="btn">Delete</button>
</form>
