<form method="post" action="{{ path('app_corpus_delete', {'id': corpu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corpu.id) }}">
    <button class="btn">Delete</button>
</form>
