{% extends 'base.html.twig' %}

{% block title %}Corpus{% endblock %}

{% block body %}
    <h1>Corpus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corpu.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ corpu.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ corpu.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ corpu.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_corpus_index') }}">back to list</a>

    <a href="{{ path('app_corpus_edit', {'id': corpu.id}) }}">edit</a>

    {{ include('corpus/_delete_form.html.twig') }}
{% endblock %}
